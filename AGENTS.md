# Agent Instructions

Before implementing a finalized plan, save the plan as a Markdown file under `codex-agent-plan/`.

Plan filenames must start with the local date-time in `YYYYMMDD_HHMMSS` format. Do not overwrite existing plan records; create a new timestamped file instead.
